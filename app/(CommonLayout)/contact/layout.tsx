import React from 'react';

const Contactlayout = ({children}:{children :React.ReactNode}) => {
    return (
        <div>
            <h1>This contact layout</h1>
            {children}
        </div>
    );
};

export default Contactlayout;